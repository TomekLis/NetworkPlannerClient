import { LatCoordinatePipe } from './lat-coordinate.pipe';

describe('LatCoordinatePipe', () => {
  it('create an instance', () => {
    const pipe = new LatCoordinatePipe();
    expect(pipe).toBeTruthy();
  });
});
