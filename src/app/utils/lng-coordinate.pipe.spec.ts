import { LngCoordinatePipe } from './lng-coordinate.pipe';

describe('LngCoordinatePipe', () => {
  it('create an instance', () => {
    const pipe = new LngCoordinatePipe();
    expect(pipe).toBeTruthy();
  });
});
